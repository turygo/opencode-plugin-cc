{
  "name": "opencode-plugin-cc",
  "version": "0.2.0",
  "private": true,
  "description": "Claude Code plugin: delegate tasks to opencode (sst/opencode) via a subagent.",
  "type": "module",
  "scripts": {
    "test": "node --test plugins/opencode/scripts/test/*.test.mjs"
  },
  "engines": {
    "node": ">=18.18"
  }
}
